server:
  port: 8010

spring:
  datasource:
    url: jdbc:mysql://10.0.181.204:13306/bs_exp_vue?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ZHTKFc5ta7tPTeyx
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 1GB
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect

app:
  file:
    upload-dir: D:/java/apache-tomcat-file/webapps/ROOT/uploads
    url-prefix: http://127.0.0.1:7080/uploads
  minio:
    endpoint: http://10.0.181.204:19000
    access-key: minio
    secret-key: KCfK6d2e7JkJWkEp
    bucket: bslab-media-dev
    url-prefix: http://10.0.181.204:19000/bslab-media-dev
    max-file-size: 1048576000
  mobile:
    enabled: true
    static-dir: ../frontend/mobile/dist
    url-prefix: /m
  dingtalk:
    # 上线开通：DINGTALK_ENABLED=true + CLIENT_ID/SECRET，并在钉钉开放平台登记 redirect_uri
    enabled: ${DINGTALK_ENABLED:false}
    client-id: ${DINGTALK_CLIENT_ID:}
    client-secret: ${DINGTALK_CLIENT_SECRET:}
    # 生产域名示例：https://school.example.com/m ；开发/LAN 示例：http://10.0.1.103:8011/m
    # 完整回调地址：{frontend-origin}/#/settings/dingtalk/callback
    frontend-origin: ${DINGTALK_FRONTEND_ORIGIN:http://10.0.1.103:8011/m}
  jwt:
    secret: ${JWT_SECRET:bs-lab-mobile-dev-secret-change-before-production-2026}
    access-token-seconds: 7200
    refresh-token-seconds: 2592000
    issuer: bs-lab-mobile
  

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
